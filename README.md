# zonolayer
Zonolayer is a Python library for modeling last-layer uncertainty in neural networks using zonotopic representations. It provides tools to bound, and visualise imprecision in a neural networks final layer.
